
# Chapter 7

## Prerequisites

use `requirements.txt` and create a new env for this project. Refer to instructions from chapter 1 and install ollama and the reranker model used in the book `ollama run dengcao/Qwen3-Reranker-4B:Q4_K_M`. The code will need access to one large language model for RAG execution. Book is using Gemini.`

## Running Weavite Local Server

The docker-compose.yml is in the repository. Installation instructions of docker - https://docs.docker.com/compose/install/

`docker compose up -d` 